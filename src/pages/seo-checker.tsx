import * as React from "react";
import MainLayout from "../components/Layout";
// markup
const SitemapExtractor = () => {
  return <MainLayout>Seo checker</MainLayout>;
};

export default SitemapExtractor;
